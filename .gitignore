# Dependencies
node_modules/

# Build output
dist/
.wrangler/

# Environment & secrets
.env
.env.test
.env.local
.env.*.local
.dev.vars

# Logs
logs/
*.log
npm-debug.log*

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# TypeScript
*.tsbuildinfo

# Test coverage
coverage/

# Migration evidence (per-run snapshots, screenshots, generated checklists)
# Produced by scripts/run-single-zone-migration.mjs + scripts/capture-zone-state.mjs
# + scripts/verify-checklist.mjs. Always per-account/per-zone, never committable.
evidence/

# E2E test output (Playwright runs). The saved-runs/ subdir is preserved
# manually for historical reference, but the rest is regenerated every run.
test/e2e-migrations/
# Legacy name; kept ignored for pre-existing local runs.
test/fortune100-integration/

# Contributor workstation scratch — never committed.
docs/internal/

# Scratch files
temp.*

# Misc
*.local

# Git worktrees (spike/feature isolation)
.worktrees/

# Local scratch / scaffolding (not part of the app)
tmp/
